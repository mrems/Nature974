<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Geronimo" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/splash_background_light</item>
        <item name="colorPrimaryVariant">@color/splash_background_light</item>
        <item name="colorOnPrimary">@color/black</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@android:color/white</item>
        <!-- Light status bar icons. -->
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
        <!-- Navigation bar color. -->
        <item name="android:navigationBarColor" tools:targetApi="l">@android:color/white</item>
        <!-- Light navigation bar icons. -->
        <item name="android:windowLightNavigationBar" tools:targetApi="o">true</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.Geronimo.Splash" parent="Theme.AppCompat.NoActionBar">
        <item name="android:statusBarColor" tools:targetApi="l">@color/splash_background_light</item>
        <item name="android:navigationBarColor" tools:targetApi="l">@color/splash_background_light</item>
        <item name="android:windowLightNavigationBar" tools:targetApi="o">false</item>
    </style>

</resources>




