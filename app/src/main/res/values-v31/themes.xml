<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Android 12+ : on conserve tout le thème d'origine et on ajoute les attributs SplashScreen -->
    <style name="Theme.Geronimo" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Couleurs principales (identiques au thème base) -->
        <item name="colorPrimary">@android:color/white</item>
        <item name="colorPrimaryVariant">@android:color/white</item>
        <item name="colorOnPrimary">@color/black</item>
        <item name="colorSecondary">@color/offer_pro_primary</item>
        <item name="colorSecondaryVariant">@color/offer_pro_primary</item>
        <item name="colorOnSecondary">@color/black</item>

        <!-- Barres système (identiques au thème base) -->
        <item name="android:statusBarColor">@color/status_bar_translucent_green</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:navigationBarColor">@color/navigation_bar_translucent_green</item>
        <item name="android:windowLightNavigationBar">true</item>

        <!-- Styles personnalisés (identiques au thème base) -->
        <item name="popupMenuStyle">@style/CustomPopupMenu</item>
        <item name="android:popupMenuStyle">@style/CustomPopupMenu</item>

        <!-- Attributs SplashScreen Android 12+ -->
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/transparent_splash_icon</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>

    <style name="Theme.Geronimo.Splash" parent="Theme.AppCompat.NoActionBar">
        <!-- Edge-to-edge : pas de bande en haut, le splash s'étend sous la barre d'état -->
        <item name="android:windowDrawsSystemBarBackgrounds">true</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowLayoutInDisplayCutoutMode">shortEdges</item>
        <item name="android:windowLightNavigationBar">true</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:enforceNavigationBarContrast">false</item>
        <item name="android:enforceStatusBarContrast">false</item>
        <!-- Important: couvre aussi la zone de la status bar avant même que le layout soit affiché -->
        <item name="android:windowBackground">@drawable/geki_splash</item>

        <!-- Attributs SplashScreen Android 12+ -->
        <item name="android:windowSplashScreenBackground">@android:color/white</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/transparent_splash_icon</item>
        <item name="android:windowSplashScreenBrandingImage">@null</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@android:color/transparent</item>
    </style>
</resources>


